import sys
input = sys.stdin.readline

num = input()
check = map(int, input().rstrip().split())
length = int(input())
num_list = map(int, input().split())
num_list.sort()
for elem in check:
    if check[]
